# Calibrated block allocation for SD3.5-medium (24 joint blocks, 0-based): N = 13 active
# blocks, N_str = 4 of them global anchors. `blocks` lists every active block;
# `global_blocks` are injected everywhere, the rest outside the editing mask.
selected_blocks_list:
  - blocks: [0, 1, 3, 4, 12, 14, 17, 18, 19, 20, 21, 22, 23]
    global_blocks: [20, 21, 22, 23]
